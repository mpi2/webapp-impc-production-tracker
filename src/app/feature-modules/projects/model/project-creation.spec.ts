import { ProjectCrea } from './crispr-attempt';
import { ProjectCreation } from './project-creation';

describe('ProjectCreation', () => {
  it('should create an instance', () => {
    expect(new ProjectCreation()).toBeTruthy();
  });
});
