import { CrisprAlleleModificationAttempt } from './crispr-allele-modification-attempt';

describe('CrisprAlleleModificationAttempt', () => {
  it('should create an instance', () => {
    expect(new CrisprAlleleModificationAttempt()).toBeTruthy();
  });
});
