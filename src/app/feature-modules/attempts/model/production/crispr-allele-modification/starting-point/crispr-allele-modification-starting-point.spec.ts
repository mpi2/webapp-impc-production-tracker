import { CrisprAlleleModificationStartingPoint } from './crispr-allele-modification-starting-point';

describe('CrisprAlleleModificationStartingPoint', () => {
  it('should create an instance', () => {
    expect(new CrisprAlleleModificationStartingPoint()).toBeTruthy();
  });
});
