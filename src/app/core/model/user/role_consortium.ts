/**
 * Basic information for a roles for each consortium that user that is logged into the application belongs to.
 */
export class RoleConsortium {
    id: number;
    roleName: string;
    consortiumName: string;
}
