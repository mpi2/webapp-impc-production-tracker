/**
 * Basic information for a roles for each work_unit that user that is logged into the application belongs to.
 */
export class RoleWorkUnit {
    id: number;
    roleName: string;
    workUnitName: string;
}
